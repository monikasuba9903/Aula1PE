
#Criando um vetor x
x <- c(4, 2, 66, 75, 23)

#Calculando a média de x
mean(x = x, na.rm = TRUE)

#Criando um vetor y
y <- c(43, 53, 77, 11, 97)

#Calculando a correlação entre x e y
cor(x = x, y = y)
